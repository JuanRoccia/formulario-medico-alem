document.addEventListener('DOMContentLoaded', function() { const captureBtn = document.getElementById('captureBtn'); const formContainer = document.getElementById('formContainer'); const imageContainer = document.getElementById('imageContainer'); captureBtn.addEventListener('click', function() { html2canvas(formContainer).then(canvas => { const imgData = canvas.toDataURL('image/png'); const img = new Image(); img.src = imgData; imageContainer.appendChild(img); }); }); });